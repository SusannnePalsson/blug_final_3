{
  "name": "blug-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently -n API,WEB \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:api": "npm run dev --prefix backend",
    "dev:web": "npm run dev --prefix frontend",
    "test:api": "npm test --prefix backend",
    "install:all": "npm ci --prefix backend && npm ci --prefix frontend",
    "db:up": "docker compose up -d db",
    "db:down": "docker compose down",
    "db:psql": "docker exec -it blug_db psql -U blug_app -d blug",
    "db:migrate": "bash database/migrate.sh"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
